import React from 'react';
import styled from 'styled-components';

interface Props {}

const Wrapper = styled.div``;

const CardSearch: React.FC<Props> = (props: Props) => {
   const {} = props;
   return <Wrapper>Testing the card search</Wrapper>;
};

export default CardSearch;
