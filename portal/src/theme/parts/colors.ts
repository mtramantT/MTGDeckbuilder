export interface Colors {
    primary: string;
    secondary: string;
    font: string;
}

const colors: Colors = {
    primary: '#f5f8fc',
    secondary: '#667182',
    font: '#EFEFEF'
};

export default colors;
