export interface AppColors {
    primary: string;
    secondary: string;
    font: string;
}

const colors: AppColors = {
    primary: '#f5f8fc',
    secondary: '#667182',
    font: '#EFEFEF'
};

export default colors;
